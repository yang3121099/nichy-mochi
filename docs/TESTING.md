# 实测记录

## 3.4.0 · 小时编号、指令备份与彩蛋（2026-09-17）

Linux/H200 上 **100 项全部通过**，耗时 150.50 秒；本机 **34 项全部通过**。新增验证涵盖北京时间跨小时和跨日计数、重启保留编号、旧索引迁移、备份中断恢复、指令原文与回执及索引对应、归档后备份保留、已有备份不覆盖，以及提醒重启去重、周五三个时段、节假日与调休工作日、提交后缀和缺失日历处理。

日期和彩蛋测试采用注入的模拟时刻，没有修改操作系统时间，也没有向正式队列提交模拟任务。真实 GPU 测试位于独立目录，继续验证文件上传、排队、失败恢复、实际 CUDA 运算和心跳让出。心跳抢占从提交到开始约 0.336 秒，测试后显存恢复 0 MiB；这是本次硬件观测。

## 3.3.0 · Nichy / Mochi 日志（2026-09-17）

Linux/H200 上 **90 项全部通过**，耗时 150.67 秒；本机 **24 项全部通过**。验证了 Nichy 提交与 Mochi 执行日志、完整日期及时区、编号索引与校验值映射、重启后编号不变、归档后不复用编号、重复指令与并发提交使用独立编号。命令行等待编号就绪后显示读取确认，文件日志和程序原始输出保留各自语义。

测试任务全部位于隔离目录，正式队列未混入模拟任务。

## 3.2.0 · 上传即提交（2026-09-17）

H200 NVL / Linux / Python 3.12.14 上 **87 项全部通过**，耗时 147.29 秒；macOS 上 **24 项全部通过**。新增自动接收、临时上传不执行、正常重启不重放、停机期间更新、发布回执中断去重、同内容重新上传、错误指令修复、兼容命令去重及独立心跳日志验证。

真实文件传输使用另一台 CPU 客户端（本机 macOS）通过 SFTP 上传业务程序和 `command.sh`；提交阶段不运行 Nichy 命令，不执行远程 shell。GPU 通过源码 `start.sh` 启动。未使用两台 GPU，也未验证 NFS 挂载；CPU 本地目录与 GPU 接头点通过文件上传连接。

SFTP 实测确认临时文件不触发运行、第一版运行时第二版取得读取回执并排队、两版顺序完成、故意失败后继续执行新文件；四个主测试任务使用同一个监听 PID，正式队列保持空白。最终文件接口回归 17 项全部通过。

CUDA 数值校验、心跳退出与显存释放通过；本轮心跳抢占从请求发布到任务启动约 0.397 秒，显存恢复 0 MiB。延迟属于当时硬件观测。


## 3.1.0 · 文件接头点（2026-09-17）

H200 NVL / Linux / Python 3.12.14 上 **79 项全部通过**，耗时 102.81 秒；macOS 上 **24 项全部通过**。其中 79 项包含 24 项可移植测试、30 项进程与队列测试、8 项完整交互、9 项文件接口测试和 8 项实际 CUDA 测试。

新版通过源码入口运行，测试前没有安装新版 Python 包。提交进程使用独立的系统 Python 和空的 CUDA 可见设备；GPU 进程使用既有 PyTorch 环境。

新增覆盖：默认路径缺失时提示并回退、自定义路径优先级、无需安装命令的 submit.sh、实时输出、command.sh / RUN / STOP、处理中断后的去重、重启不重放、无效 shell 拒绝、既有日志及心跳文件保留、空队列也有就绪日志。

心跳数值测试完成 304 次矩阵乘法；正式任务抢占心跳从发布到开始约 0.415 秒；清理后显存回到 0 MiB。外部 GPU 进程出现后心跳提前结束，外部进程继续运行。这些均为本次硬件观测，不是普遍的延迟保证。

实际 `/users/nichy/code/start` 在可访问实例和本机均不存在，因此无法读取用户在其他环境中的既有日志。默认回退逻辑经过测试；真实跨主机共享挂载、NFS 故障及 MIG 仍未实测。

## 3.0.0 历史记录

测试日期：2026-09-16。版本：3.0.0。

## 结果

| 环境 | 测试数 | 结果 |
|---|---:|---|
| macOS，Python 3.14，独立虚拟环境 | 22 | 全部通过 |
| Linux，Python 3.12.14，NVIDIA H200 NVL | 68 | 全部通过，88.29 秒 |

Linux 68 项由 22 项提交/对话/心跳规则测试、30 项队列与进程测试、8 项完整交互测试、8 项实际 CUDA 测试组成。CUDA 环境为 PyTorch 2.11.0+cu128、CUDA 12.8。

另外验证了可编辑安装生成的 `nichy` 短命令：一个 SSH 会话运行 `start.sh`，另一个会话使用清空 CUDA 可见设备的提交环境，执行 `nichy run --wait hello.py`、状态查看、日志查看和正常停止。实际收到以下对话：

```text
Mochi 记下了：hello.py · 版本 58bd455b
Mochi 收到了：hello.py · 版本 58bd455b
Mochi 正在运行：hello.py · 版本 58bd455b
Mochi 完成了 ✓：hello.py · 版本 58bd455b
```

## 关键验证

- 同名内容的幂等底层提交、并发提交、串行执行、失败后继续下一任务。
- 快照发布完整性、源文件修改后旧版本不变、错误或被修改的快照不执行。
- 新任务在旧任务运行期间获得独立读取回执，同时显示两个内容版本。
- 快速任务也记录回执；离线不误报收到；过期 worker 不误报正在运行。
- 取消、超时、正常停止、排空、重启、故障锁、`UNKNOWN` 不自动重跑。
- worker 的 SIGTERM、SIGKILL、SIGSTOP；脱离会话及忽略终止信号的后代清理；无关进程存活。
- 自选且含空格的队列路径；`start.sh` 首次初始化；已有配置不覆盖；业务 `start.sh` 可正常提交。
- 配置只在启动时读取，运行期间修改不会生效；重启后应用；错误配置暂停心跳而正式任务继续。
- CUDA_VISIBLE_DEVICES 为空时不扩大设备可见性；不可用指标跳过心跳。

## CUDA 实测数据

| 场景 | 观测 |
|---|---|
| 2 秒心跳数值校验 | 316 次矩阵乘法，约 48 MiB 张量，结果正确 |
| 正式任务抢占心跳 | 从发布到开始约 0.446 秒；先清理心跳进程再启动任务 |
| 外部 GPU 进程出现 | 心跳提前退出，外部进程继续运行；本次心跳运行约 1.04 秒 |
| 用户后台任务让出并恢复 | 2 次执行，50 个 checkpoint 分块无缺失、无重复 |
| 计算、抢占后的显存 | 测试前 0 MiB，清理后 0 MiB |

这些是此硬件和当时环境的观测，不是所有机器上的延迟保证。

## 隔离和范围

自动测试使用每个用例单独的队列、程序目录和日志。大多数 Linux 模拟提交通过系统 Python 独立进程执行，环境只包含基础 PATH、语言及空的 CUDA 可见设备；执行端使用 GPU 虚拟环境。测试任务没有提交到最终常驻服务的正式队列。

本次验证的目录位于同一容器，独立 SSH 会话也连接同一实例。真实跨主机 NFS 的缓存延迟、断网、服务器失效和挂载一致性未在本次验证范围内。多卡选择策略通过规则测试验证；实际 CUDA 测试使用一张 H200。MIG 和非 NVIDIA 设备没有实际验证。

没有模拟宿主机重启、驱动故障或断电；这些情况应按管理员文档确认旧进程和结果后恢复。文件队列需要支持原子重命名、硬链接和目录 fsync 的 POSIX 文件系统。

## 复现

```bash
python3 tests/run.py --report test-report.json
python3 tests/run.py --gpu --report gpu-test-report.json
```

GPU 测试请使用已分配、当前空闲的设备。设置 `GPUQ_TEST_BASE` 可指定测试目录，`GPUQ_KEEP_TESTS=1` 保留证据，`GPUQ_CLIENT_PYTHON` 指定独立提交端 Python。不设置时使用测试目录下的临时文件夹并在结束后清理。
